import React, {FC} from "react";
import {SafeAreaView, Text} from "react-native";

const Profile: FC = () => {
    return (
        <SafeAreaView>
            <Text>Profile</Text>
        </SafeAreaView>
    );
}

export default Profile;